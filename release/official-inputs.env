# Pinned upstream inputs consumed by the official Matchbox OS image build.
# These MUST be digest-pinned refs (never floating tags).
# Update via PR when sw-ourbox-os publishes a new official bundle.
#
# To re-resolve current digests:
#   oras resolve ghcr.io/techofourown/sw-ourbox-os/platform-contract:edge
#   oras resolve ghcr.io/techofourown/sw-ourbox-os/airgap-platform:edge-arm64

PLATFORM_CONTRACT_REF=ghcr.io/techofourown/sw-ourbox-os/platform-contract@sha256:8df5587d396ee75e77ddb8f159e26db9335976c0ce241b5be1812ecbbf1cb360
AIRGAP_PLATFORM_REF=ghcr.io/techofourown/sw-ourbox-os/airgap-platform@sha256:e1c5e7f3e754c0498be77f76c1105df1791d591ed99b91b92213f0c8297014ec

# INSTALL_DEFAULTS_REF is omitted until the installer build is confirmed to bake
# in fallback defaults from that bundle.
