# tools/versions.env
# Pinned versions for reproducible OurBox Matchbox OS builds.
# Change ONLY deliberately, and update docs/OPS.md "Last verified" after re-verifying end-to-end.

K3S_VERSION=v1.35.0+k3s1

# Demo workload baked into the image as an offline example.
NGINX_IMAGE=docker.io/library/nginx:1.27-alpine

# File sharing + notes web UI baked into the image
DUFS_IMAGE=docker.io/sigoden/dufs:v0.42.0
FLATNOTES_IMAGE=docker.io/dullage/flatnotes:v5.0.0

# BuildKit version installed by tools/bootstrap-host.sh (host-side)
BUILDKIT_VERSION=v0.23.2
