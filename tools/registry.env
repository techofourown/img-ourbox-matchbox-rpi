# Centroid local registry
REGISTRY="registry.benac.dev"
REGISTRY_NAMESPACE="ourbox"

# Optional: where your CA cert lives on hosts that will pull/push
REGISTRY_CA_CERT="/etc/ssl/centroid-ca/certs/ca-root.crt"
